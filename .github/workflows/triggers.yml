name: working with triggers
on:
  push:
    branches: main
  pull_request:
    branches: main
  schedule:
     - cron: '46 5 * * *'
  workflow_dispatch:
  
jobs:
  checking:
    runs-on: ubuntu-latest
    steps:
      - name: check trigger
        run: echo "checking triggers"
